# Block semua akses HTTP ke folder config
# Berisi credentials dan security config — tidak boleh diakses publik
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
